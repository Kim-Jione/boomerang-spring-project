<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bumerang.domain.jobSearch.JobSearchDao">

    <select id="findById"
            resultType="com.example.bumerang.domain.jobSearch.JobSearch">
        SELECT * FROM job_search
        WHERE job_id=#{jobId}
    </select>

    <select id="findAll"
            resultType="com.example.bumerang.domain.jobSearch.JobSearch">
        SELECT *
        FROM job_search
    </select>

    <insert id="insert">
        INSERT INTO
        job_search (job_content_title, job_content, job_genre, job_art_title, job_start_date, job_production_date, job_to, job_pay, job_field, job_gender, job_contact, job_deadline, user_id)
        VALUES
        (#{jobContentTitle}, #{jobContent}, #{jobGenre}, #{jobArtTitle}, #{jobStartDate}, #{jobProductionDate}, #{jobTo}, #{jobPay}, #{jobField}, #{jobGender}, #{jobContact}, #{jobDeadline}, #{userId})
    </insert>

    <update id="update">
        UPDATE job_search
        SET
        job_content_title = #{jobContentTitle},
        job_content = #{jobContent},
        job_genre = #{jobGenre},
        job_art_title = #{jobArtTitle},
        job_start_date = #{jobStartDate},
        job_production_date = #{jobProductionDate},
        job_to = #{jobTo},
        job_field = #{jobField},
        job_gender = #{jobGender},
        job_contact = #{jobContact},
        job_deadline = #{jobDeadline}
        WHERE
        job_id = #{jobId}
        AND user_id = #{userId}
    </update>

    <delete id="delete">
        UPDATE job_search
        SET
        job_status = 'deleted'
        WHERE job_id = #{jobId}
    </delete>

    <select id="writeResult"
            resultType="com.example.bumerang.domain.jobSearch.JobSearch">
        SELECT *
        FROM job_search
        WHERE user_id = #{userId}
        ORDER BY job_id desc
        FETCH NEXT 1 ROWS ONLY
    </select>
</mapper>